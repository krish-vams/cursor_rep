spring:
  application:
    name: property-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:travelgraph}?currentSchema=property_schema
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: property_schema
        jdbc:
          time_zone: UTC
        format_sql: true
  flyway:
    enabled: true
    schemas: property_schema
    default-schema: property_schema
    locations: classpath:db/migration

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

# graphql-kotlin defaults: /graphql endpoint, /subscriptions websocket.
# GraphiQL and introspection are gated by profile (see GraphQLConfiguration / dev profile).
graphql:
  packages:
    - "com.travelgraph.property.graphql"
  endpoint: "graphql"
  graphiql:
    enabled: false
  introspection:
    enabled: false

logging:
  level:
    root: INFO
    com.travelgraph: INFO
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [${spring.application.name:},%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n"
